class Bot {
  final String username;
  final String name;
  final String locale;
  final String flagFilename;
  final String faceFilename;

  const Bot(this.username, this.name, this.locale, this.flagFilename, this.faceFilename);
}